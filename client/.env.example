# The client needs no map keys any more.
#
# Map configuration is delivered at runtime from the server's /api/config
# endpoint, so keys live in server/.env and can be rotated with a restart
# instead of a rebuild.
#
# Only set this if the API is hosted somewhere other than the same origin.
# REACT_APP_API_URL=https://your-api.example.com/api
