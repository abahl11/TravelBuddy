# Environment variables live in the workspace they belong to:
#
#   server/.env   API configuration (MONGO_URI, JWT_SECRET, ...)
#   client/.env   build-time React configuration (REACT_APP_*)
#
# Copy the matching .env.example in each folder and fill it in.
# On Render, set them in the service's Environment tab instead of committing them.
